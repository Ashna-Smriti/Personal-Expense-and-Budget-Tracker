# ─── Required for production ───
# Set these via Render dashboard / Railway env vars.
# The in-memory MongoDB (mongodb-memory-server) is for local dev only.
# MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/budget-tracker
JWT_SECRET=budget-tracker-super-secret-key-2024
JWT_EXPIRES_IN=7d
PORT=5001

# Google OAuth — required for Google Sign-In to work
# GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
